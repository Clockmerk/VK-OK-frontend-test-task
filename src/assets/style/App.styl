#root 
  margin 0 auto
  padding 1rem
  text-align center

main 
  min-width 320px
  display flex
  justify-content: center
  gap 10px

.box-counters, .box-buttons 
  display grid;
  gap 1rem;
  grid-template-columns repeat(auto-fit, minmax(100px, 150px));

